# Copy to .env and fill values
# Direct Line secret from Copilot Studio: Settings -> Channels -> Direct Line (or Web channel security secrets)
DIRECT_LINE_SECRET=REPLACE_WITH_YOUR_SECRET

# Direct Line base URL. For global: https://directline.botframework.com
# For regional bots you may need a regional endpoint (e.g. India): https://india.directline.botframework.com
DIRECT_LINE_BASE_URL=https://directline.botframework.com

# Optional: restrict which origins can call the token endpoint (comma-separated).
# For same-origin hosting, leave empty.
ALLOWED_ORIGINS=

# Server
PORT=3000
